# Bharat Gas WhatsApp Message Sender - frontend environment
# Copy to .env.local and fill in your own value. Never commit real credentials.

# Base URL of the backend API (must also be allowed by ALLOWED_ORIGINS on the backend).
NEXT_PUBLIC_API_URL=http://localhost:3001